#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int l,b;
    scanf("%d %d",&l,&b);
    
    printf("Required length is %.2fm\n",(float)(l+l)+(b+b));
    printf("Required quantity of carpet is %.2fsqm",(float)l*b);
    return 0;
}
